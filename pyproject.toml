[project]
name = "optisort"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "eth-gre", email = "outlook.enthusiast@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
optisort = "optisort:main"

[tool.uv.build-backend]
module-root = "rust"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
